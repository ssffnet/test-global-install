{
  "name": "test-global-install",
  "version": "1.0.0",
  "description": "Minimal package to test ",
  "main": "dist/test.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" &&  exit 0",
    "prepareLInux": "rm -rf dist && sleep 5s && mkdir dist && sleep 5s && cp src/test.js dist/test.js",
    "prepare": "copy src\\test.js dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ssffnet/test-global-install.git"
  },
  "author": "me",
  "license": "ISC",
  "dependencies": {},
  "bin": {
    "my-test": "dist/test.js"
  }
}